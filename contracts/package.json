{
  "name": "tokentap-contracts",
  "version": "1.0.0",
  "description": "TokenTap faucet smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network holesky",
    "fund": "hardhat run scripts/fund-faucet.js --network holesky",
    "mint": "hardhat run scripts/mint-and-fund.js --network holesky",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.4.5"
  }
} 